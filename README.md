# 🏨 Mini Booking — Fullstack Reservation System

> Тестовое задание: Вертикальный срез системы бронирования отелей. Реализован полный цикл разработки: Backend (Node.js) + Frontend (React) +
> Mobile/Desktop (Flutter).

![License](https://img.shields.io/badge/license-MIT-blue) ![Stack](https://img.shields.io/badge/stack-Node.js_|_React_|_Flutter-orange)
![Docker](https://img.shields.io/badge/docker-ready-green)

## 📋 Описание проекта

Система позволяет бронировать номера в отелях с проверкой пересечения дат.

**Ключевые особенности:**

- **Единый GraphQL API** для всех клиентов.
- **Real-time обновление**: Изменения на вебе мгновенно видны в мобилке (при обновлении списка).
- **Conflict Resolution**: Серверная валидация предотвращает двойное бронирование (Overbooking).
- **Cross-platform**: Веб-клиент + Мобильное приложение (Android/iOS) + Windows виджет.

---

## 🛠 Технологический стек

### 🔙 Backend

- **Runtime**: Node.js
- **API**: Apollo Server (GraphQL)
- **Database**: PostgreSQL
- **ORM**: Prisma (Schema & Migrations)
- **Language**: TypeScript

### 🌐 Web Frontend

- **Core**: React + Vite
- **State/Network**: Apollo Client
- **Styling**: Tailwind CSS
- **Language**: TypeScript

### 📱 Mobile & Desktop

- **Framework**: Flutter
- **Platforms**: Android, iOS, Windows
- **Network**: GraphQL Client

---

## 🚀 Быстрый старт (Docker)

Для запуска серверной части и веб-клиента требуются установленные **Docker Desktop** и **Node.js** (нужен только для запуска скриптов
автоматизации).

### 1. Клонирование

Выполните в терминале: `git clone https://github.com/darckday2010/task_4.git` `cd task_4`

### 2. Запуск одной командой (One-Click Setup)

Я подготовил скрипт, который выполняет полный цикл запуска: очистка, поднятие контейнеров, ожидание БД и заливка тестовых данных.

Запустите команду: `yarn local:reset`

**Что происходит под капотом:**

1. Останавливаются и удаляются старые контейнеры.
2. Поднимаются PostgreSQL, Backend и Frontend.
3. Скрипт ждет 5 секунд инициализации БД.
4. Выполняется `prisma migrate reset` (создание таблиц и заливка Seed-данных).

_(Команда выполняется через Docker, локальная установка зависимостей в папках backend/frontend не требуется)._

### 3. Готово!

Проект доступен по адресам:

- 🌍 **Web App:** http://localhost:5173 — Основной интерфейс.
- 📡 **GraphQL Playground:** http://localhost:4000 — Документация API.
- 🗄️ **Prisma Studio:** `cd backend && npx prisma studio` (порт 5555) — Админка БД.

---

## 📱 Запуск мобильного приложения (Flutter)

Мобильное приложение запускается локально (вне Docker) для доступа к эмуляторам.

### Предварительные требования

1. Установлен **Flutter SDK**.
2. Запущен **Backend** (см. шаг выше).
3. Запущен эмулятор или подключено реальное устройство.

### Инструкция

1. Откройте новый терминал в корне проекта и перейдите в папку: `cd mobile`

2. Установите зависимости: `flutter pub get`

3. **Запустите приложение:**
    - Автоматический выбор: `flutter run`
    - Windows-виджет (Desktop): `flutter run -d windows`

> **⚠️ Важно для Android Emulator:** Android-эмулятор не видит `localhost` компьютера напрямую. Чтобы приложение смогло подключиться к API,
> выполните в отдельном терминале: `adb reverse tcp:4000 tcp:4000`

---

## 🧪 Сценарии проверки (Test Scenarios)

Чтобы проверить работоспособность системы согласно требованиям ТЗ:

### ✅ Сценарий 1: Валидация бронирования

1. Откройте **Web** (http://localhost:5173).
2. Выберите **Grand Budapest Hotel** -> Номер **101**.
3. Попробуйте выбрать даты, которые уже помечены красным (заняты seed-данными).
4. Нажмите "Забронировать" — система покажет ошибку или не даст выбрать даты.

### ✅ Сценарий 2: Синхронизация платформ

1. Откройте **Mobile** приложение.
2. Зайдите в тот же отель и номер.
3. Забронируйте свободные даты в **Web** версии.
4. Нажмите кнопку "Назад" и снова зайдите в номер в **Mobile** версии (или обновите экран).
5. Даты, забронированные на вебе, станут недоступны на телефоне.

### ✅ Сценарий 3: Отмена брони

1. В **Mobile** нажмите на иконку "Корзина" у любой активной брони.
2. Подтвердите удаление.
3. Обновите страницу в **Web** — бронь исчезнет, номер снова станет свободен.

---

## 📂 Структура проекта

- **backend/** — Серверная часть
    - `prisma/` — Схема БД и скрипт seed.ts
    - `src/` — Resolvers, TypeDefs (GraphQL)
- **frontend/** — Веб-клиент (React + Vite)
    - `src/api/` — GraphQL запросы и мапперы данных
    - `src/pages/` — Страницы приложения
- **mobile/** — Мобильное приложение (Flutter)
    - `lib/core/` — Сетевой слой
    - `lib/ui/` — Экраны и виджеты
- **docker-compose.yml** — Оркестрация контейнеров
- **package.json** — Скрипты для управления проектом

---

## 🆘 Troubleshooting (Решение проблем)

**1. Ошибка `connect ECONNREFUSED 127.0.0.1:5432`** База данных еще не успела инициализироваться.

- **Решение:** Подождите 10-15 секунд и запустите команду инициализации вручную: `yarn local:db:init`

**2. Список отелей пуст** Контейнеры запущены, но миграции не применились.

- **Решение:** Выполните `yarn local:db:init`

**3. Mobile: Бесконечная загрузка (Spinner)** Приложение не может достучаться до сервера.

- **Решение (Android):** Проверьте, выполнили ли вы `adb reverse tcp:4000 tcp:4000`.
- **Решение (Общее):** Убедитесь, что сервер доступен по http://localhost:4000 в браузере.

---

## ⚖️ Архитектурные решения и компромиссы (Trade-offs)

Поскольку это тестовое задание (MVP), некоторые решения были приняты в пользу скорости разработки и удобства проверки, в ущерб
"Enterprise-стандартам".

### 1. Почему `.env` файлы в репозитории?

**В реальном проекте:** Файлы конфигурации всегда находятся в `.gitignore`. Секреты доставляются через CI/CD или Secret Managers. **В этом
проекте:** Файлы `.env` намеренно закоммичены.

> **Цель:** Обеспечить **Zero-configuration start**. Чтобы вам не пришлось создавать файлы вручную и гадать, какие переменные нужны для
> запуска. Скачал — запустил.

### 2. Почему Prisma + Apollo Server, а не NestJS + TypeORM?

Для реализации вертикального среза (Vertical Slice) использован легковесный подход на базе **Node.js + Apollo Server**.

- **Prisma:** Дает отличную типобезопасность из коробки и ускоряет написание схемы БД.
- **Отказ от NestJS:** В рамках небольшого тестового задания NestJS добавил бы много бойлерплейта (модули, декораторы, DI), который
  избыточен для 2-х сущностей.

### 🚀 Как бы мы сделали это на реальном Production-проекте:

Если бы этот проект разрабатывался для бизнеса с планами на масштабирование, стек и подходы были бы изменены:

| Аспект         | Тестовое задание (MVP)   | Production Ready решение                                                       |
| :------------- | :----------------------- | :----------------------------------------------------------------------------- |
| **Framework**  | Vanilla Node.js + Apollo | **NestJS**. Для строгой модульности, DI и стандартизированной архитектуры.     |
| **Database**   | Prisma (прямые вызовы)   | **TypeORM** или Prisma внутри **Repository Pattern**. Для абстракции и тестов. |
| **Config**     | `.env` в Git             | **Vault / AWS Secrets**. Строгий запрет на секреты в репо.                     |
| **Docker**     | Обычные образы           | **Multi-stage builds**. Alpine образы + Nginx.                                 |
| **Validation** | Базовая                  | **Class-validator + DTO**. Глобальные пайпы.                                   |
| **Testing**    | Ручное                   | **Unit + E2E**. Автотесты и CI пайплайны.                                      |

---

**Author:** [Bronepuh]
